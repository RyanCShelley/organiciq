import { PhaseStub } from "@/components/PhaseStub";

export default function ActivityPage() {
  return (
    <PhaseStub
      title="Activity"
      description="Activity history (recommendations, tasks, annotations, syncs) arrives with later phases."
    />
  );
}
