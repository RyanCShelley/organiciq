import { PhaseStub } from "@/components/PhaseStub";

export default function ClientStrategyPage() {
  return (
    <PhaseStub
      title="Client Strategy"
      description="Goals, business context, and organic strategy move into Organic IQ in Phase 11."
    />
  );
}
